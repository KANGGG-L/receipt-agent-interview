# 07 · 组件 Spec · AI 治理、多模型灰度发布与评测控制台

> **模块定位**：系统 AI 基础设施治理与质量保证控制台。承载多模型引擎热插拔适配（本地 CLI / 云端网关）、精细化灰度发布分流路由（按单据概率 / 按供应商 Hash），以及基于 57 张香港真实复杂单据的 **黄金样本基准评测中心**。  
> **对标 14 步方案**：`step6-技术可行性`、`step9-AI技术方案`、`step12-测试验收`、`step14-上线迭代`。  
> **实现代码**：[`api_admin.py`](file:///Users/ethan/Documents/GitHub/receipt-agent-interview/demo/app/api_admin.py)、[`app/llm.py`](file:///Users/ethan/Documents/GitHub/receipt-agent-interview/demo/app/llm.py)、[`templates/admin.html`](file:///Users/ethan/Documents/GitHub/receipt-agent-interview/demo/templates/admin.html)  

---

## 1. 业务背景与设计动机

- **多模型生态演进快，避免厂商绑定**：
  - 各大模型提供商（开源端侧、云端多模态、专有 API）更迭迅速，模型单价与识别能力持续变化；
  - 系统必须做到 **“模型可插拔、切换不改管线代码”**。
- **财务级系统上线必须可灰度、可回归**：
  - 新模型上线前，必须通过标准基准测试集自动化回归，确认准确率与门禁通过率达标；
  - 线上放量必须支持平滑灰度分配，支持单据级别与供应商级别的精细流量隔离。

---

## 2. 模型引擎可插拔统一抽象 ([`app/llm.py`](file:///Users/ethan/Documents/GitHub/receipt-agent-interview/demo/app/llm.py))

所有接入模型全部继承自 LangChain 的 `BaseChatModel`，对编排管线暴露统一的 `invoke` 接口：

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                              统一模型引擎适配抽象层                                    │
├────────────────────────────────────────────────────────────────────────────────────────┤
│                                BaseChatModel (LangChain)                               │
│                                           ▲                                            │
│        ┌──────────────────────────────────┼──────────────────────────────────┐         │
│        │                                  │                                  │         │
│  OpencodeChatModel              CodeBuddyChatModel                   OpenAIChatModel   │
│  (本地免费 CLI 进程调用)        (本地免费 CLI 进程调用)             (云端标准 API 网关)│
│  · MiMo-V2.5 Free 多模态        · MiniMax-M3 视觉模型               · Qwen-VL / GPT-4o │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 灰度发布分流机制 (Canary Routing Spec)

系统在管理后台提供常规生产组（Production）与灰测组（Canary）的完全参数隔离，支持两种分流策略：

```mermaid
flowchart TD
    Req[进货单据解析请求] --> CheckEnable{后台是否开启灰度?<br/>grey_enabled == true}
    
    CheckEnable -->|否| RouteProd[100% 走常规组引擎]
    CheckEnable -->|是| CheckMode{灰度分配模式 grey_assign_mode}
    
    CheckMode -->|模式1: receipt (单据概率)| Rand[生成随机数 random.random * 100]
    Rand --> HitRand{随机数 < 设定概率%?}
    HitRand -->|是| RouteGrey[命中灰测组引擎]
    HitRand -->|否| RouteProd
    
    CheckMode -->|模式2: supplier (供应商Hash)| HashCalc[计算 md5(supplier_name) % 100]
    HashCalc --> HitHash{Hash桶位 < 设定概率%?}
    HitHash -->|是| RouteGrey
    HitHash -->|否| RouteProd
```

#### 双模式设计考量：
1. **单据随机模式 (`receipt`)**：适合大流量下无偏探索模型性能，评估全品类泛化能力；
2. **供应商 Hash 模式 (`supplier`)**：保证同一供应商的历史单据始终由同一组模型处理，保证识别习惯与 VendorMemory 的稳定性与连贯性。

---

## 4. 黄金样本集评测中心 (Golden Samples Benchmark)

### 4.1 评测数据集构成
系统内置 **57 张真实香港单据**构成的黄金基准评测集（覆盖 7 种单据形态）：
- **手写街市单**：22 张（含草书、繁体俗称、涂改、混写）；
- **传统印刷送货单**：15 张（含手写勾选、手写补录金额）；
- **电子磅单/热敏纸单**：10 张（含毛重/净重折算、折痕褪色）；
- **现场更正单/折让单 (CN)**：6 张；
- **月结对账单 (Statement)**：4 张。

每张单据均附带经人工精细校验的 **Ground Truth JSON** 标准答案。

---

### 4.2 评测指标矩阵与自动化输出报告

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                      黄金样本集批量自动化评测输出矩阵                                  │
├──────────────────────┬──────────────────────┬──────────────────────────────────────────┤
│ 评估维度             │ 达标基线要求         │ 评测说明                                 │
├──────────────────────┼──────────────────────┼──────────────────────────────────────────┤
│ **关键字段召回率**   │ $\ge 96.0\%$         │ 供应商名、日期、总金额匹配精准度         │
├──────────────────────┼──────────────────────┼──────────────────────────────────────────┤
│ **明细行完整度**     │ $\ge 95.0\%$         │ 品名、数量、单价、小计全字段准确度       │
├──────────────────────┼──────────────────────┼──────────────────────────────────────────┤
│ **算术门禁拦截率**   │ **$100.0\%$**        │ 面对大模型算术差错时的 100% 代码拦截率   │
├──────────────────────┼──────────────────────┼──────────────────────────────────────────┤
│ **端到端解析时延**   │ $\le 60\text{s}$     │ 从发起识别到 Verified Draft 输出平均耗时 │
├──────────────────────┼──────────────────────┼──────────────────────────────────────────┤
│ **单据 Token 成本**  │ 结合模型单价量化     │ 统计每张单据平均消耗 Input/Output Token  │
└──────────────────────┴──────────────────────┴──────────────────────────────────────────┘
```

---

## 5. API 接口契约一览

| 方法 | 路径 | 入参 | 返回 / 行为 | 权限 |
| :--- | :--- | :--- | :--- | :--- |
| `GET` | `/api/admin/engine_config` | 无 | 获取常规组与灰测组模型配置、当前灰度参数 | **Admin 独占** |
| `POST` | `/api/admin/engine_config` | `EngineConfigPayload` | 更新模型引擎与灰度分流规则 | **Admin 独占** |
| `POST` | `/api/admin/run_benchmark` | `model_type, sample_ids` | 触发黄金样本集批量自动化评测 | **Admin 独占** |
| `GET` | `/api/admin/benchmark_report`| `run_id: str` | 获取最新评测基准报告及各项关键指标看板 | **Admin 独占** |
