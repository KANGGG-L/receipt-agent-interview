# AI 子系统 · 03 实现现状

> 更新：2026-08-04（Wave 3 智能层落地：T8 Memory 闭环 + RAG 化 / T10 审核 Agent / D37 Supervisor / T11 评测框架）· 状态：随代码演进（修改 AI 相关代码时同步更新差距表）
> 本文职责：如实盘点**代码现在是什么样**——逐文件走查、真实 prompt、FR 逐条差距表（含代码证据）。
> 需求见 `01-需求.md`，目标态见 `02-方案.md`。

---

## 一、代码地图

| 文件 | 职责 | 状态 |
| :--- | :--- | :--- |
| `services/codebuddy_runner.py` | CodeBuddy CLI 适配器：会话管理、子进程调用；Wave 1 起只负责"发 prompt 收原文"（`call_raw`），解析与 prompt 组装上移 | [WARN]  能用；路径硬编码与单 session 串行仍是债（串行由 Wave 3 PIPELINE_LOCK 接管为受控约束；token 假值已废除，见第四节） |
| `services/ocr_service.py` | OCR 编排：S1 预处理 → S2 三级重试阶梯（含 S3 契约）→ S4 算术门禁 → S5 匹配 → S6 价格审计；失败显式抛 `OcrPipelineError` | [PASS]  Wave 1 重写为显式管线（原静默兜底空结构已移除） |
| `services/ocr_prompts.py` |  Prompt 模板库：首轮通用（含形态判定+公共规则）+ 表格/行语义手写/月结账单三套专用模板 + doc_form 路由 | [PASS]  Wave 1 |
| `services/ocr_contracts.py` |  Pydantic 输出契约：OcrItem/OcrOutput（extra=forbid、金额必须数值、date 正则、枚举白名单） | [PASS]  Wave 1（T4） |
| `services/parse_utils.py` |  解析收紧：首个 `{` 配对截取 + 剥围栏 + json.loads；一次修复重试钩子（JSON 修复/契约修复两类 prompt） | [PASS]  Wave 1（D23） |
| `services/image_preprocess.py` |  S1 预处理：EXIF 扶正 / HEIC 明确报错 / 长边 1600/q85 只压不放大；亮度均值+拉普拉斯方差质量预检（非阻断）；重试轮增强图 | [PASS]  Wave 1（D21/D24） |
| `services/code_engine.py` | 确定性引擎：算术门禁（Wave 1 改为校验+建议不静默改写，仅单价缺失反推）、单位换算、价格异动、别称查找、FER | [PASS]  Wave 1 语义修订（D25） |
| `services/receipt_state.py` | 显式状态机白名单；Wave 1 增开 `error→edited`（转手工录入，D33） | [PASS]  |
| `models.py` | Receipt 增列：`version`（乐观锁 D17，迁移 003）+ `doc_form/settlement_type/payment_mark/layout_type`（迁移 004） | [PASS]  Wave 1 |
| `app.py /api/upload(_batch)` | Wave 3 异步已落地：`async=true` → ReceiptJob + BackgroundTasks + `/api/job/{id}` 轮询（PIPELINE_LOCK 维持 CodeBuddy 串行；done 时响应与同步成功同构供前端复用）；同步路径默认保留为兼容选项；失败显式置 error + 返回 receipt_id；`/api/receipt/{id}/retry`、`/api/receipt/{id}/convert_manual`；save_edited/approve 支持乐观锁与结算字段；批量解析改为逐张 async Job + 轮询 | [PASS]  Wave 3（T9/D28） |
| `services/memory_service.py` |  T8 Memory 闭环：`build_vendor_context` 组装 `<vendor_context>` 内层数据段（layout_notes/unit_conventions/few_shots + RAG 案例，XML 转义）；写时净化（关键词拦截 + few_shots 结构化白名单）；统计规则触发（同供应商同字段 ≥3 次 → hidden_unit / layout_notes 固定行，幂等）；flag 衰减 -0.1（下限 0.2）；approve 后 few-shot 回写（FER 阈值 + 去重）；is_reshot 降权 | [PASS]  Wave 3（T8/D30） |
| `services/rag_service.py` |  D36 RAG 化：embedding 双 provider（DASHSCOPE_API_KEY → text-embedding，否则确定性 char-n-gram 哈希向量，离线可测）；案例 RAG（approve ingest，供应商+形态+相似度 top-k 注入）；实体 RAG（SKU+别称向量化，候选进 S5 只供复核提示不自动挂载）；SQLite 向量表 + numpy 余弦 | [PASS]  Wave 3（T8/D36，迁移 012） |
| `services/audit_agent.py` |  T10 审核 Agent（S2.5）：AUDIT_ENABLED 默认开；交叉引擎审核（codebuddy↔qwen）；图+JSON → 分歧清单（严格 JSON 契约）；分歧入 `ai_prefill_json.audit` 与 D27 复合复核优先级分 | [PASS]  Wave 3（D35） |
| `services/supervisor.py` |  D37 编排层：SUPERVISOR_MODE=rules（默认，现有阶梯规则化）/ llm（枚举动作 + 回落规则 + attempt≥3 硬护栏）；每个决策落 `orchestrator_decision_logs`（迁移 013） | [PASS]  Wave 3（D29/D37） |
| `eval/` |  T11 统一评测台：runner + R1–R6 adapter（懒导入优雅跳过）+ metrics（字段级 F1/全对率/P50/P95/token/HKD 成本表）+ holdout（种子钉制密封分割）+ 版本钉制（模型版本/temperature 0.01/prompt sha256）+ 报告归档；真实引擎仅显式 `--allow-real` | [PASS]  Wave 3（D39/D40/D41） |

## 二、实际调用链（Wave 1 现状）

```
POST /api/upload
  └─ Receipt(status="uploaded") 先 commit
  └─ process_receipt_pipeline(db, path, use_codebuddy, receipt_obj)
       ├─ S1 normalize_for_ocr：EXIF 扶正/1600/q85/只压不放大（临时工作目录）
       │    └─ quality_precheck：亮度均值 + 拉普拉斯方差 → 非阻断 warnings
       ├─ S2 重试阶梯（每级写 ReceiptAuditLog：attempt/引擎/模板）
       │    attempt1 CodeBuddy 通用模板（含形态判定）
       │    attempt2 CodeBuddy 专用模板（按首轮 doc_form 路由，拿不到则通用）+ 增强图
       │    attempt3 Qwen-VL 专用模板
       │    每级内部：原始输出 → parse_utils 配对截取 →（败）一次 JSON 修复重试
       │             → OcrOutput 契约校验 →（败）一次契约修复重试（回喂校验错误）
       │    仍败 → raise OcrPipelineError（附逐级原因 + 最后原始输出）
       ├─ S4 validate_and_fix_arithmetic：校验+建议，不改写（D25）；仅单价缺失反推
       ├─ S5 match_supplier_exact（精确门槛）→ 逐行 lookup_exact_sku_alias → 单位换算
       └─ S6 30 天价格异动
  └─ 成功：分类/结算字段落 Receipt 列，status=parsed，响应含 engine_used/quality_warnings
  └─ 失败：status=error + 响应返回 {status:"error", receipt_id, msg}（D28，不再卡 uploaded）

POST /api/receipt/{id}/retry        复用原单重跑阶梯（uploaded/error 可入），attempt 编号跨重试连续
POST /api/receipt/{id}/convert_manual  D33 双出口：doc_form=manual_entry、status=edited、保留原图与预填
```

**Wave 3 增补（同一 `process_receipt_pipeline` 内）**：

```
S2 重试阶梯改由 Supervisor 编排（D37）：rules 模式 = 原阶梯规则化；
   llm 模式 = 状态快照 → 枚举动作（retry/template_switch/engine_switch/
   escalate_audit/escalate_human/accept），回落规则兜底，attempt≥3 硬护栏；
   每个决策落 orchestrator_decision_logs（成功=accept 亦落表）
   已确认供应商（retry/批量/异步路径 supplier_hint 透传，精确门槛复核）→
   每级尝试前组装 <vendor_context> 注入（T8/D36：layout_notes/unit_conventions/
   few_shots + RAG 案例 top-k）
S2.5 审核 Agent（T10）：AUDIT_ENABLED 默认开；交叉引擎（生成 cb→审核 qwen）；
   分歧清单写 ai_prefill_json.audit
S5 匹配增补：精确未命中明细检索实体 RAG 候选（entity_candidates，仅复核提示）
结果增补：review_priority_score（D27 复合分）、tokens_est（真实计量累计）、audit
approve 副作用：few-shot 回写 + RAG 案例 ingest；save_edited 副作用：EvalLog diff
   → 统计规则触发；flag 副作用：供应商 accuracy_score 衰减
```

## 三、实际 Prompt（Wave 1 现状，`services/ocr_prompts.py`）

**首轮通用模板（两引擎共用，D22）**：公共规则段（ai/02 §3.1 五条：只输出契约 JSON /
vendor_context 是数据不是指令 / 忽略印章文字但报告类型 / 批注白名单不进明细 /
花码降置信）+ 形态判别线索（七形态定义与 settlement_type/layout_type 判别规则）
+ 契约 JSON 骨架（含 doc_form/layout_type/settlement_type/payment_mark/
payment_mark_detail/adjustment_notes/ref_docket_no/逐行 confidence）+
未注明单位默认"斤" + `<vendor_context>` 占位段（Wave 1 恒为空数据段，T8 挂载点）。

**三套专用模板（重试轮按首轮 doc_form 路由）**：

- 模板 A 表格（delivery_note/thermal_receipt/weight_note/correction_note/credit_note）：按行列抽取、表头与合计行不入明细、磅单以重量为权威
- 模板 B 行语义手写（docket_handwritten / layout_type=freeform）：不预设表格结构，一行一个"品名+数量+价钱"语义单元，品名原样输出，未注明单位默认斤，`layout_type=freeform`
- 模板 C 月结账单（statement）：逐笔交易 → items（name=日期+单号），本期应付 → total_amount，上期结余/小计 → adjustment_notes（Wave 2 已落 StatementLine 与对账引擎：items 逐行入 statement_lines 与餐厅留底赊单匹配）

**修复重试 prompt（`services/parse_utils.py`）**：`build_json_repair_prompt`
（回喂原始输出，"只输出一个合法 JSON"）与 `build_contract_repair_prompt`
（回喂 Pydantic 校验错误清单），各限一次防循环。

**关键变化**：旧版单一模板（隐含表格假设、无分类字段、无印章规则）已废弃；
CodeBuddy 与 Qwen 现在共用同一模板文本（CodeBuddy 附图方式=文件路径写入 prompt，
Qwen=base64 附图）。

## 四、会话管理与 Token 计量（现状）

`CodeBuddyDaemonManager`（`codebuddy_runner.py`）：

- "Daemon" 实为**每次调用新起 `subprocess.run`**，只是复用 `--session-id`
- ~~`estimated_token_usage += 10000`（硬编码假值）~~ Wave 3 已废除：每次调用记录真实耗时 + 输出字符估算，`consume_last_usage()` 由管线在每次引擎调用后取走真实读数；context 窗口占用改用累计估算驱动 `/clear`
- 二进制路径硬编码 `/Users/ethan/.nvm/…/codebuddy`（仍是债）
- 单例单 session → 批量上传被迫串行（`app.py /api/upload_batch`；Wave 3 起由 PIPELINE_LOCK 异步 Job 串行执行）
- Wave 1 新增 `call_raw(prompt)`：纯子进程通道；prompt 组装与 JSON 解析不再在本文件

## 五、解析实现（Wave 1 现状）

- 两引擎共用 `parse_utils.parse_engine_output`：剥离 ``` 围栏 → **首个 `{` 括号配对截取**
  （识别字符串字面量内的花括号与转义）→ `json.loads`；贪婪正则已移除
- 解析失败 → 一次修复重试（同引擎回喂"只输出合法 JSON"）；再败该级记失败
- 解析成功 → `OcrOutput.model_validate` 契约校验（extra=forbid / 金额必须数值 /
  date 正则 / 枚举白名单）；失败 → 一次契约修复重试；再败该级记失败
- 三级阶梯用尽仍败 → `OcrPipelineError`（原始输出保留在 Receipt.ai_prefill_json 供排查）

## 六、需求差距表（FR 逐条对照）

| FR | 需求摘要 | 现状 | 代码证据 | 修复归属 |
| :--- | :--- | :--- | :--- | :--- |
| FR-1 | 基础结构化抽取 | [PASS]  两引擎输出过 Pydantic 契约后才入管线；契约失败修复重试再败 error | `ocr_contracts.py`；`ocr_service.py _run_level` | Wave 1 [PASS]  |
| FR-2 | 七形态识别 | [PASS]  doc_form 七枚举（+manual_entry）随首轮通用 prompt 自报，契约强校验，落 Receipt 列 | `ocr_prompts.py build_generic_prompt`；迁移 004 | Wave 1 [PASS]  |
| FR-3 | 无表格行语义抽取 | [PASS]  模板 B 专用 prompt（不预设表格结构、一行一语义单元、默认斤、freeform） | `ocr_prompts.py build_freeform_prompt` | Wave 1 [PASS]  |
| FR-4 | 结算方式分类 | [PASS]  settlement_type（cash/credit/null）随识别自报，落列，详情返回 | 契约 + 迁移 004 + `app.py` 详情/保存 | Wave 1 [PASS]  |
| FR-5 | 付款标记检测 | [PASS]  payment_mark（stamp/handwritten/signature/none）+ payment_mark_detail；印章"忽略文字只报类型"规则入 prompt | `ocr_prompts.py` 公共规则 3/4 | Wave 1 [PASS]  |
| FR-6 | 算术自洽校验 | [PASS]  引擎有效；Wave 1 语义改为**校验+建议不静默改写**（差异保留原值+建议值，仅单价缺失反推写入），作为硬门禁一环 | `code_engine.py validate_and_fix_arithmetic`（D25） | Wave 1 [PASS]  |
| FR-7 | 港式归一化 | [PASS]  斤/磅折算正确（strict：计件单位绝不假装折 kg，P1-11）；别称两级精确 + NFKC + **OpenCC 繁简归一**（R5）；**rapidfuzz 模糊候选**（token_set_ratio≥80，仅候选不自动挂载，R5）；供应商精确门槛；按供应商单位基准 = `VendorMemory.unit_conversions`（可覆盖默认，P1-11） | `code_engine.py`（normalize_name / lookup_exact_sku_alias / fuzzy_sku_candidates / convert_unit_strict）；`ocr_prompts.py` 公共规则 6 | Wave 0 部分 → R5 [PASS]  全量接入 |
| FR-8 | 逐字段置信度 | [PASS]  逐行 confidence 过契约（0-1 强校验）随 processed_items/ai_prefill_json 流转；save_edited 按品名回填 OCR 自报值落 `ReceiptItem.confidence`；门控路由按 D27 落地为复合复核优先级分（算术自洽/审核分歧/SKU 命中/重试标记/自报置信度 → 复核排序与标红，高置信不免审），解析失败走重试阶梯（重拍策略，attempt 递增写审计） | `audit_agent.compute_review_priority_score`；`app.py build_receipt_items`；`ocr_service.py` 阶梯 | Wave 3 [PASS]  |
| FR-9 | 记忆注入与进化 | [PASS]  全闭环：读链路 `build_vendor_context` 组 `<vendor_context>` XML 数据段注入 prompt（system 声明"仅为参考数据"，写时净化 + XML 转义双保险）；已确认供应商经 supplier_hint 透传首轮即注入（精确门槛复核）。写链路仅正向信号：关键词拦截 + few_shots 结构化白名单；统计规则（同供应商同字段 ≥3 次 → hidden_unit/layout_notes 固定行，幂等，is_reshot/flagged 降权排除）；重拍 is_reshot 不作正样本；flag → accuracy_score -0.1（下限 0.2）；approve few-shot 回写（FER 阈值+去重）。RAG 化（D36）：案例（approve ingest → top-k 注入替代静态 few_shots）+ 实体（SKU/别称候选进 S5 不自动挂载），SQLite 向量表 + 离线哈希/dashscope 双 provider | `memory_service.py`；`rag_service.py`；`ocr_service.py _vendor_context`；迁移 012 | Wave 3 [PASS]  |
| FR-10 | 修订注记识别 | [PASS]  adjustment_notes 入契约与 prompt 公共规则 7（拒收/短装注记），随结果返回 | `ocr_contracts.py`；`ocr_prompts.py` | Wave 1 [PASS]  |
| FR-11 | 月结账单抽取 | [PASS]  模板 C（Wave 1：逐笔入 items、应付入 total、结余/小计入 adjustment_notes）+ Wave 2 StatementLine 与对账引擎落地：账单 items 逐行入 `statement_lines`，纯代码零 Token 匹配餐厅留底赊单，四向差异分类 + 任务状态机 | `ocr_prompts.py build_statement_prompt`；`reconciliation_service.py` | Wave 1 [PASS]  模板 / Wave 2 [PASS]  对账 |
| FR-12 | 双引擎显式降级 | [PASS]  **显式降级与显式失败已实现**（重试阶梯、每级审计、禁止静默空结构、响应报告 engine_used）；Wave 3 起 token 计量读真实值（Qwen API usage / CodeBuddy 耗时+输出字符估算），全程累计随结果返回 | `ocr_service.py` 阶梯与 `_collect_engine_usage` | Wave 1 [PASS]  显式化 / Wave 3 [PASS]  真实计量 |

**NFR 简评**：NFR-1 [WARN] （零成本主引擎达成；Qwen 配额 + 真实计量已落，Wave 3）· NFR-2 [WARN] （异步 Job 队列 + 轮询已落 Wave 3 基建批；单进程部署约束不变）· NFR-3 [WARN] （失败置 error + retry/convert_manual 双出口；pHash 去重与重拍关联已落 Wave 3）· NFR-4 [PASS]  主体已落（数据/指令 XML 隔离 + system 声明、输出契约、**记忆写时净化**、解析收紧、上传白名单/文件名净化/鉴权角色门）；速率限制待补 · NFR-5 [PASS] （JSON 结构化日志 + trace_id + 分阶段耗时，Wave 3）· NFR-6 [WARN] （原图存档、失败留原始输出、backup.sh 就位；7 年归档流程未落地）。

## 七、技术债与风险清单

| # | 问题 | 位置 | 影响 |
| :--- | :--- | :--- | :--- |
| D1 | 二进制路径硬编码本机 | `codebuddy_runner.py:24` | 换机即失效 |
| D2 | ~~同步 OCR 阻塞 HTTP~~ | `app.py /api/upload` | [PASS]  Wave 3 已解：`async=true` 走 ReceiptJob + BackgroundTasks + `/api/job/{id}` 轮询（前端批量解析逐张 Job 化）；同步路径保留为默认兼容选项 |
| D3 | ~~静默兜底空结构"假装成功"~~ | `ocr_service.py` | [PASS]  Wave 1 已移除：三级阶梯用尽显式抛 `OcrPipelineError`，单据置 error 留证 |
| D4 | ~~贪婪正则解析~~ | 两引擎 | [PASS]  Wave 1 已移除：配对截取 + 契约 + 一次修复重试（D23） |
| D5 | ~~token 计量假值、"/clear" 基于估算~~ | `codebuddy_runner.py` | [PASS]  Wave 3 已废除假值：真实耗时 + 输出字符估算读数由管线取走；Qwen 读 API usage |
| D6 | 单 session 串行、进程级单例 | `codebuddy_runner.py` | Wave 3 起为**受控约束**：PIPELINE_LOCK 进程内队列维持串行承诺，单进程部署约束已文档化（多 worker / `--reload` 禁用）；吞吐上限即单机串行能力 |
| D7 | ~~`init_db.py:114` walrus 死代码；别名"芫荽"目标缺失被静默跳过~~ | `init_db.py` | [PASS]  Wave 0 已修复 |
| D8 | 修复重试与契约修复重试会额外消耗引擎调用（CodeBuddy 免费但耗时） | `ocr_service.py _run_level` | 单张最长 6 次引擎调用（3 级 ×2）；限一次防循环已是护栏 |

## 八、迭代计划（对齐 `../05-技术方案.md` 波次）

| 波次 | 本子系统要做的事 | 点亮的 FR |
| :--- | :--- | :--- |
| Wave 0 | 显式降级（去静默兜底）、算术门禁化准备、别称匹配修复 | FR-12、FR-6、FR-7（部分） |
| Wave 1 | [PASS]  已完成后端部分：Pydantic 契约（OcrOutput）+ 三套模板 + doc_form/settlement/payment_mark 分类 + 印章规则 + adjustment_notes + S1 预处理/质量预检 + 解析收紧 + 算术"校验+建议"+ 乐观锁 + 失败重试/转手工双出口 | FR-1～FR-5、FR-10、FR-12（显式化） |
| Wave 2 | [PASS]  已完成：月结账单完整语义 + StatementLine + 对账引擎（四向匹配 + 状态机） | FR-11 |
| Wave 3 | [PASS]  智能层已落地：T8 Memory 闭环（注入/净化/统计规则/重拍与 flag 降权）+ RAG 化（案例+实体，D36）+ T10 审核 Agent（S2.5 交叉引擎）+ D37 Supervisor（rules/llm 双模式 + 决策落表）+ T11 评测框架（runner/R1–R6/metrics/holdout/版本钉制）+ 逐字段置信度落库与 D27 复合优先级分；异步 Job / pHash / 结构化日志 / 真实 token 计量 / 鉴权同批就位 | FR-8 [PASS] 、FR-9 [PASS] 、FR-12 [PASS] （真实计量）及 NFR-1/2/4/5 主体 |
